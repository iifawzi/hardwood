/*
 *  SPDX-License-Identifier: Apache-2.0
 *
 *  Copyright The original authors
 *
 *  Licensed under the Apache Software License version 2.0, available at http://www.apache.org/licenses/LICENSE-2.0
 */
package dev.hardwood.internal.predicate;

import java.util.function.IntUnaryOperator;

import dev.hardwood.reader.FilterPredicate.Operator;
import dev.hardwood.reader.RowReader;
import dev.hardwood.schema.FileSchema;

import static dev.hardwood.internal.predicate.RecordFilterCompiler.compareBinary;
import static dev.hardwood.internal.predicate.RecordFilterCompiler.indexedTopLevel;

/// Indexed-access counterpart to [RecordFilterFusion]. Used when the
/// reader is a [RowReader] (so the cast in the fused lambda is safe) and
/// both leaves operate on directly-addressable top-level columns. The
/// `topLevelFieldIndex` mapping returns the reader's field index for a
/// given file leaf-column index, or `-1` when the column isn't directly
/// addressable.
///
/// This file is generated by `tools/gen_fusion.py` — do not hand-edit.
final class RecordFilterFusionIndexed {

    private RecordFilterFusionIndexed() {
    }

    static RowMatcher tryFuseAnd2(ResolvedPredicate a, ResolvedPredicate b, FileSchema schema, IntUnaryOperator topLevelFieldIndex) {
        if (a instanceof ResolvedPredicate.LongPredicate la && b instanceof ResolvedPredicate.LongPredicate lb) {
            int iA = indexedTopLevel(schema, la.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, lb.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            if (la.columnIndex() == lb.columnIndex()) return idxLongRangeAnd(iA, la.op(), la.value(), lb.op(), lb.value());
            return idxLongLongAndDiff(iA, la.op(), la.value(), iB, lb.op(), lb.value());
        }
        if (a instanceof ResolvedPredicate.IntPredicate ia && b instanceof ResolvedPredicate.IntPredicate ib) {
            int iA = indexedTopLevel(schema, ia.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, ib.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            if (ia.columnIndex() == ib.columnIndex()) return idxIntRangeAnd(iA, ia.op(), ia.value(), ib.op(), ib.value());
            return idxIntIntAndDiff(iA, ia.op(), ia.value(), iB, ib.op(), ib.value());
        }
        if (a instanceof ResolvedPredicate.DoublePredicate da && b instanceof ResolvedPredicate.DoublePredicate db) {
            int iA = indexedTopLevel(schema, da.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, db.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            if (da.columnIndex() == db.columnIndex()) return idxDoubleRangeAnd(iA, da.op(), da.value(), db.op(), db.value());
            return idxDoubleDoubleAndDiff(iA, da.op(), da.value(), iB, db.op(), db.value());
        }
        if (a instanceof ResolvedPredicate.BooleanPredicate ba && b instanceof ResolvedPredicate.BooleanPredicate bb) {
            int iA = indexedTopLevel(schema, ba.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, bb.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxBooleanBooleanAndDiff(iA, ba.op(), ba.value(), iB, bb.op(), bb.value());
        }
        if (a instanceof ResolvedPredicate.BinaryPredicate ba && b instanceof ResolvedPredicate.BinaryPredicate bb) {
            int iA = indexedTopLevel(schema, ba.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, bb.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            if (ba.columnIndex() == bb.columnIndex()) return idxBinaryRangeAnd(iA, ba.op(), ba.value(), bb.op(), bb.value(), ba.signed());
            return idxBinaryBinaryAndDiff(iA, ba.op(), ba.value(), ba.signed(), iB, bb.op(), bb.value(), bb.signed());
        }
        if (a instanceof ResolvedPredicate.LongPredicate la && b instanceof ResolvedPredicate.DoublePredicate db) {
            int iA = indexedTopLevel(schema, la.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, db.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxLongDoubleAndDiff(iA, la.op(), la.value(), iB, db.op(), db.value());
        }
        if (a instanceof ResolvedPredicate.DoublePredicate da && b instanceof ResolvedPredicate.LongPredicate lb) {
            int iA = indexedTopLevel(schema, lb.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, da.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxLongDoubleAndDiff(iA, lb.op(), lb.value(), iB, da.op(), da.value());
        }
        if (a instanceof ResolvedPredicate.IntPredicate ia && b instanceof ResolvedPredicate.LongPredicate lb) {
            int iA = indexedTopLevel(schema, ia.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, lb.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxIntLongAndDiff(iA, ia.op(), ia.value(), iB, lb.op(), lb.value());
        }
        if (a instanceof ResolvedPredicate.LongPredicate la && b instanceof ResolvedPredicate.IntPredicate ib) {
            int iA = indexedTopLevel(schema, ib.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, la.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxIntLongAndDiff(iA, ib.op(), ib.value(), iB, la.op(), la.value());
        }
        if (a instanceof ResolvedPredicate.IntPredicate ia && b instanceof ResolvedPredicate.DoublePredicate db) {
            int iA = indexedTopLevel(schema, ia.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, db.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxIntDoubleAndDiff(iA, ia.op(), ia.value(), iB, db.op(), db.value());
        }
        if (a instanceof ResolvedPredicate.DoublePredicate da && b instanceof ResolvedPredicate.IntPredicate ib) {
            int iA = indexedTopLevel(schema, ib.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, da.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxIntDoubleAndDiff(iA, ib.op(), ib.value(), iB, da.op(), da.value());
        }
        return null;
    }

    static RowMatcher tryFuseOr2(ResolvedPredicate a, ResolvedPredicate b, FileSchema schema, IntUnaryOperator topLevelFieldIndex) {
        if (a instanceof ResolvedPredicate.LongPredicate la && b instanceof ResolvedPredicate.LongPredicate lb) {
            int iA = indexedTopLevel(schema, la.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, lb.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            if (la.columnIndex() == lb.columnIndex()) return idxLongRangeOr(iA, la.op(), la.value(), lb.op(), lb.value());
            return idxLongLongOrDiff(iA, la.op(), la.value(), iB, lb.op(), lb.value());
        }
        if (a instanceof ResolvedPredicate.IntPredicate ia && b instanceof ResolvedPredicate.IntPredicate ib) {
            int iA = indexedTopLevel(schema, ia.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, ib.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            if (ia.columnIndex() == ib.columnIndex()) return idxIntRangeOr(iA, ia.op(), ia.value(), ib.op(), ib.value());
            return idxIntIntOrDiff(iA, ia.op(), ia.value(), iB, ib.op(), ib.value());
        }
        if (a instanceof ResolvedPredicate.DoublePredicate da && b instanceof ResolvedPredicate.DoublePredicate db) {
            int iA = indexedTopLevel(schema, da.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, db.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            if (da.columnIndex() == db.columnIndex()) return idxDoubleRangeOr(iA, da.op(), da.value(), db.op(), db.value());
            return idxDoubleDoubleOrDiff(iA, da.op(), da.value(), iB, db.op(), db.value());
        }
        if (a instanceof ResolvedPredicate.BooleanPredicate ba && b instanceof ResolvedPredicate.BooleanPredicate bb) {
            int iA = indexedTopLevel(schema, ba.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, bb.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxBooleanBooleanOrDiff(iA, ba.op(), ba.value(), iB, bb.op(), bb.value());
        }
        if (a instanceof ResolvedPredicate.BinaryPredicate ba && b instanceof ResolvedPredicate.BinaryPredicate bb) {
            int iA = indexedTopLevel(schema, ba.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, bb.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            if (ba.columnIndex() == bb.columnIndex()) return idxBinaryRangeOr(iA, ba.op(), ba.value(), bb.op(), bb.value(), ba.signed());
            return idxBinaryBinaryOrDiff(iA, ba.op(), ba.value(), ba.signed(), iB, bb.op(), bb.value(), bb.signed());
        }
        if (a instanceof ResolvedPredicate.LongPredicate la && b instanceof ResolvedPredicate.DoublePredicate db) {
            int iA = indexedTopLevel(schema, la.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, db.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxLongDoubleOrDiff(iA, la.op(), la.value(), iB, db.op(), db.value());
        }
        if (a instanceof ResolvedPredicate.DoublePredicate da && b instanceof ResolvedPredicate.LongPredicate lb) {
            int iA = indexedTopLevel(schema, lb.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, da.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxLongDoubleOrDiff(iA, lb.op(), lb.value(), iB, da.op(), da.value());
        }
        if (a instanceof ResolvedPredicate.IntPredicate ia && b instanceof ResolvedPredicate.LongPredicate lb) {
            int iA = indexedTopLevel(schema, ia.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, lb.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxIntLongOrDiff(iA, ia.op(), ia.value(), iB, lb.op(), lb.value());
        }
        if (a instanceof ResolvedPredicate.LongPredicate la && b instanceof ResolvedPredicate.IntPredicate ib) {
            int iA = indexedTopLevel(schema, ib.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, la.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxIntLongOrDiff(iA, ib.op(), ib.value(), iB, la.op(), la.value());
        }
        if (a instanceof ResolvedPredicate.IntPredicate ia && b instanceof ResolvedPredicate.DoublePredicate db) {
            int iA = indexedTopLevel(schema, ia.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, db.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxIntDoubleOrDiff(iA, ia.op(), ia.value(), iB, db.op(), db.value());
        }
        if (a instanceof ResolvedPredicate.DoublePredicate da && b instanceof ResolvedPredicate.IntPredicate ib) {
            int iA = indexedTopLevel(schema, ib.columnIndex(), topLevelFieldIndex);
            int iB = indexedTopLevel(schema, da.columnIndex(), topLevelFieldIndex);
            if (iA < 0 || iB < 0) return null;
            return idxIntDoubleOrDiff(iA, ib.op(), ib.value(), iB, da.op(), da.value());
        }
        return null;
    }

    // ==================== generated lambda bodies ====================

    private static RowMatcher idxLongLongAndDiff(int iA, Operator opA, long vA, int iB, Operator opB, long vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
        };
    }

    private static RowMatcher idxLongLongOrDiff(int iA, Operator opA, long vA, int iB, Operator opB, long vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
        };
    }

    private static RowMatcher idxLongRangeAnd(int i, Operator opA, long vA, Operator opB, long vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA && v >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA && v >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA && v >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA && v >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA && v >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA && v >= vB; };
            };
        };
    }

    private static RowMatcher idxLongRangeOr(int i, Operator opA, long vA, Operator opB, long vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v == vA || v >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v != vA || v >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v < vA || v >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v <= vA || v >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v > vA || v >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; long v = a.getLong(i); return v >= vA || v >= vB; };
            };
        };
    }

    private static RowMatcher idxIntIntAndDiff(int iA, Operator opA, int vA, int iB, Operator opB, int vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
        };
    }

    private static RowMatcher idxIntIntOrDiff(int iA, Operator opA, int vA, int iB, Operator opB, int vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getInt(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getInt(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getInt(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getInt(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getInt(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getInt(iB) >= vB; };
            };
        };
    }

    private static RowMatcher idxIntRangeAnd(int i, Operator opA, int vA, Operator opB, int vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA && v >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA && v >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA && v >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA && v >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA && v >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA && v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA && v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA && v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA && v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA && v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA && v >= vB; };
            };
        };
    }

    private static RowMatcher idxIntRangeOr(int i, Operator opA, int vA, Operator opB, int vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v == vA || v >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v != vA || v >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v < vA || v >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v <= vA || v >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v > vA || v >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA || v == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA || v != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA || v < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA || v <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA || v > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; int v = a.getInt(i); return v >= vA || v >= vB; };
            };
        };
    }

    private static RowMatcher idxDoubleDoubleAndDiff(int iA, Operator opA, double vA, int iB, Operator opB, double vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) != 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) != 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) != 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) != 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) != 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) != 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) == 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) == 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) == 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) == 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) == 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) == 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) >= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) >= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) >= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) >= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) >= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) >= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) > 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) > 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) > 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) > 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) > 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) > 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) <= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) <= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) <= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) <= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) <= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) <= 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) < 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) < 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) < 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) < 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) < 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (Double.compare(a.getDouble(iA), vA) < 0) return false; if (a.isNull(iB)) return false; return Double.compare(a.getDouble(iB), vB) >= 0; };
            };
        };
    }

    private static RowMatcher idxDoubleDoubleOrDiff(int iA, Operator opA, double vA, int iB, Operator opB, double vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) == 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) == 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) == 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) == 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) == 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) == 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) != 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) != 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) != 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) != 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) != 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) != 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) < 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) < 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) < 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) < 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) < 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) < 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) <= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) <= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) <= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) <= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) <= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) <= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) > 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) > 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) > 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) > 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) > 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) > 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) >= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) >= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) >= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) >= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) >= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && Double.compare(a.getDouble(iA), vA) >= 0) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
        };
    }

    private static RowMatcher idxDoubleRangeAnd(int i, Operator opA, double vA, Operator opB, double vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 && Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 && Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 && Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 && Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 && Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 && Double.compare(v, vB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 && Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 && Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 && Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 && Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 && Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 && Double.compare(v, vB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 && Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 && Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 && Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 && Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 && Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 && Double.compare(v, vB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 && Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 && Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 && Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 && Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 && Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 && Double.compare(v, vB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 && Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 && Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 && Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 && Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 && Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 && Double.compare(v, vB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 && Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 && Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 && Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 && Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 && Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 && Double.compare(v, vB) >= 0; };
            };
        };
    }

    private static RowMatcher idxDoubleRangeOr(int i, Operator opA, double vA, Operator opB, double vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 || Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 || Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 || Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 || Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 || Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) == 0 || Double.compare(v, vB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 || Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 || Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 || Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 || Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 || Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) != 0 || Double.compare(v, vB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 || Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 || Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 || Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 || Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 || Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) < 0 || Double.compare(v, vB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 || Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 || Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 || Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 || Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 || Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) <= 0 || Double.compare(v, vB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 || Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 || Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 || Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 || Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 || Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) > 0 || Double.compare(v, vB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 || Double.compare(v, vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 || Double.compare(v, vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 || Double.compare(v, vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 || Double.compare(v, vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 || Double.compare(v, vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; double v = a.getDouble(i); return Double.compare(v, vA) >= 0 || Double.compare(v, vB) >= 0; };
            };
        };
    }

    private static RowMatcher idxBooleanBooleanAndDiff(int iA, Operator opA, boolean vA, int iB, Operator opB, boolean vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) != vA) return false; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) != vA) return false; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) != vA) return false; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) != vA) return false; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) != vA) return false; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) != vA) return false; return !a.isNull(iB); };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) == vA) return false; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) == vA) return false; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) == vA) return false; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) == vA) return false; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) == vA) return false; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getBoolean(iA) == vA) return false; return !a.isNull(iB); };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; return !a.isNull(iB); };
            };
        };
    }

    private static RowMatcher idxBooleanBooleanOrDiff(int iA, Operator opA, boolean vA, int iB, Operator opB, boolean vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) == vA) return true; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) == vA) return true; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) == vA) return true; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) == vA) return true; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) == vA) return true; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) == vA) return true; return !a.isNull(iB); };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) != vA) return true; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) != vA) return true; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) != vA) return true; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) != vA) return true; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) != vA) return true; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getBoolean(iA) != vA) return true; return !a.isNull(iB); };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB) && a.getBoolean(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB) && a.getBoolean(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA)) return true; return !a.isNull(iB); };
            };
        };
    }

    private static RowMatcher idxBinaryBinaryAndDiff(int iA, Operator opA, byte[] vA, boolean signedA, int iB, Operator opB, byte[] vB, boolean signedB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) != 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) != 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) != 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) != 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) != 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) != 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) == 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) == 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) == 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) == 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) == 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) == 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) >= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) >= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) >= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) >= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) >= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) >= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) > 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) > 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) > 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) > 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) > 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) > 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) <= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) <= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) <= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) <= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) <= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) <= 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) < 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) < 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) < 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) < 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) < 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA)) return false; if (compareBinary(a.getBinary(iA), vA, signedA) < 0) return false; if (a.isNull(iB)) return false; return compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
        };
    }

    private static RowMatcher idxBinaryBinaryOrDiff(int iA, Operator opA, byte[] vA, boolean signedA, int iB, Operator opB, byte[] vB, boolean signedB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) == 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) == 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) == 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) == 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) == 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) == 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) != 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) != 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) != 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) != 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) != 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) != 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) < 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) < 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) < 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) < 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) < 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) < 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) <= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) <= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) <= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) <= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) <= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) <= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) > 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) > 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) > 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) > 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) > 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) > 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) >= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) >= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) >= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) >= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) >= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && compareBinary(a.getBinary(iA), vA, signedA) >= 0) return true; return !a.isNull(iB) && compareBinary(a.getBinary(iB), vB, signedB) >= 0; };
            };
        };
    }

    private static RowMatcher idxBinaryRangeAnd(int i, Operator opA, byte[] vA, Operator opB, byte[] vB, boolean signed) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 && compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 && compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 && compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 && compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 && compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 && compareBinary(v, vB, signed) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 && compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 && compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 && compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 && compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 && compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 && compareBinary(v, vB, signed) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 && compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 && compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 && compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 && compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 && compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 && compareBinary(v, vB, signed) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 && compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 && compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 && compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 && compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 && compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 && compareBinary(v, vB, signed) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 && compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 && compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 && compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 && compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 && compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 && compareBinary(v, vB, signed) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 && compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 && compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 && compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 && compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 && compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 && compareBinary(v, vB, signed) >= 0; };
            };
        };
    }

    private static RowMatcher idxBinaryRangeOr(int i, Operator opA, byte[] vA, Operator opB, byte[] vB, boolean signed) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 || compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 || compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 || compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 || compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 || compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) == 0 || compareBinary(v, vB, signed) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 || compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 || compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 || compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 || compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 || compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) != 0 || compareBinary(v, vB, signed) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 || compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 || compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 || compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 || compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 || compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) < 0 || compareBinary(v, vB, signed) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 || compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 || compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 || compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 || compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 || compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) <= 0 || compareBinary(v, vB, signed) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 || compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 || compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 || compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 || compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 || compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) > 0 || compareBinary(v, vB, signed) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 || compareBinary(v, vB, signed) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 || compareBinary(v, vB, signed) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 || compareBinary(v, vB, signed) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 || compareBinary(v, vB, signed) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 || compareBinary(v, vB, signed) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(i)) return false; byte[] v = a.getBinary(i); return compareBinary(v, vA, signed) >= 0 || compareBinary(v, vB, signed) >= 0; };
            };
        };
    }

    private static RowMatcher idxLongDoubleAndDiff(int iA, Operator opA, long vA, int iB, Operator opB, double vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getLong(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
        };
    }

    private static RowMatcher idxLongDoubleOrDiff(int iA, Operator opA, long vA, int iB, Operator opB, double vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getLong(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
        };
    }

    private static RowMatcher idxIntLongAndDiff(int iA, Operator opA, int vA, int iB, Operator opB, long vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
        };
    }

    private static RowMatcher idxIntLongOrDiff(int iA, Operator opA, int vA, int iB, Operator opB, long vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) == vB; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) != vB; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) < vB; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) <= vB; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) > vB; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && a.getLong(iB) >= vB; };
            };
        };
    }

    private static RowMatcher idxIntDoubleAndDiff(int iA, Operator opA, int vA, int iB, Operator opB, double vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) != vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) == vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) >= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) > vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) <= vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (a.isNull(iA) || a.getInt(iA) < vA) return false; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
        };
    }

    private static RowMatcher idxIntDoubleOrDiff(int iA, Operator opA, int vA, int iB, Operator opB, double vB) {
        return switch (opA) {
            case EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) == vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case NOT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) != vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) < vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case LT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) <= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) > vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
            case GT_EQ -> switch (opB) {
                case EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) == 0; };
                case NOT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) != 0; };
                case LT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) < 0; };
                case LT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) <= 0; };
                case GT -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) > 0; };
                case GT_EQ -> row -> { RowReader a = (RowReader) row; if (!a.isNull(iA) && a.getInt(iA) >= vA) return true; return !a.isNull(iB) && Double.compare(a.getDouble(iB), vB) >= 0; };
            };
        };
    }

}
